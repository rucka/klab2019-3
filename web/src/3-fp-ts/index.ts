// import { emailREx, urlREx, vatcodeREx } from '../_stuff/regex-collection'
// import { valOf, checkedOf } from '../_stuff/dom'

export const processCustomer = () => {
  return true as (true | string[])
}
